import styled from 'styled-components';

export const RadioButtonInInput = styled.div`
	display: flex;
	width: calc(100% + 25px);
`;

export const RadioButton = styled.input`
	position: relative;
	right: 30px;
`;
